<div>
  <% @students.each do |student| %>
    <div>
      <%= link_to student.to_s, student_path(student) %></div>
      <!-- <a href= "/student/1"> THIS IS SAME AS link_to-->
    <!--<%= student.first_name + " " + student.last_name %> -->
  <% end %>
</div>
